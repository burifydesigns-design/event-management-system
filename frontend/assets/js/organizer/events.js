// Create/edit/delete events.
// Example create: await apiRequest('/events', { method: 'POST', body: { title, startsAt, venue } });
// Example delete: await apiRequest(`/events/${id}`, { method: 'DELETE' });
